<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_home_path(@home), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @home.name %></h2>
  <p><%= @home.address %></p>
</div>
